---
- name: normalize-claim-results teardown
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Report no-op destroy status
      ansible.builtin.set_stats:
        data:
          destroy_status: skipped
          destroy_results_json: "[]"
        per_host: false
