---
- name: reset-rack-password teardown
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: Export no-op destroy outputs
      ansible.builtin.set_stats:
        data:
          destroy_status: successful
          destroy_results_json: "[]"
        per_host: false
