---
- name: prepare-claim-target-credentials teardown
  hosts: localhost
  connection: local
  gather_facts: false
  tasks:
    - name: No teardown actions required for prepare-claim-target-credentials
      ansible.builtin.debug:
        msg: "prepare-claim-target-credentials does not manage remote resources during destroy"
